export const MIN_WIDTH = 20;

export const SLIDER_MARGIN = 5;
export const SLIDER_LINE = 1;
export const SLIDER_WIDTH = (SLIDER_MARGIN * 2) + SLIDER_LINE;
export const SLIDER_HALF_WIDTH = SLIDER_WIDTH / 2;
